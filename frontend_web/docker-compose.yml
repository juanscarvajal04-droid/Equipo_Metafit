services:
  metafit-app:
    container_name: metafit_frontend
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # <--- OJO: Asegúrate de que esta línea esté exacta
    environment:
      - CHOKIDAR_USEPOLLING=true