# ============================================================
# MetaFit — Orquestación de Servicios
# Archivo: /docker-compose.yml  (raíz del proyecto)
# Uso:     docker compose up --build
#
# BD: imagen mariadb:11 (motor de producción). Docker ejecuta todos
#      los .sql de ./database/ dentro de /docker-entrypoint-initdb.d/
#      en orden LEXICOGRÁFICO, que coincide con las DEPENDENCIAS:
#        01_estructura.sql         -> tablas base
#        02_migracion_movil.sql    -> tablas app móvil   (depende de 01)
#        03_mejoras_estructura.sql -> columnas + tablas  (depende de 01 y 02)
#        04_datos_iniciales.sql    -> TODOS los datos    (depende de 01, 02 y 03)
#        05_password_reset.sql     -> tabla recuperación (depende de 01)
# ============================================================

services:

  # ──────────────────────────────────────────────────────────
  # SERVICIO 1: Base de Datos MariaDB 11
  # ──────────────────────────────────────────────────────────
  db:
    image: mariadb:11
    container_name: metafit_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      # Forzar utf8mb4 desde arranque para evitar problemas de encoding
      MYSQL_INITDB_SKIP_TZINFO: "true"
    ports:
      - "3306:3306"
    volumes:
      # Se monta la CARPETA completa. Docker ejecuta TODOS los .sql en
      # orden lexicográfico, que ya coincide con las dependencias:
      #   01_estructura -> 02_migracion_movil -> 03_mejoras_estructura
      #   -> 04_datos_iniciales -> 05_password_reset
      # Los scripts solo se ejecutan si el volumen de datos está vacío
      # (primer arranque). Para re-ejecutarlos: docker compose down -v
      - ./database:/docker-entrypoint-initdb.d:ro
      # Persistencia de datos entre reinicios
      - metafit_db_data:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mariadb-admin", "ping", "--skip-ssl", "-h", "127.0.0.1", "-u", "root", "-p${DB_PASSWORD}" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s
    networks:
      - metafit_net

  # ──────────────────────────────────────────────────────────
  # SERVICIO 2: Backend Node.js
  # ──────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: metafit_backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: ${PORT}
      DB_HOST: db # <- nombre del servicio Docker, NO localhost
      DB_PORT: 3306
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-8h}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://127.0.0.1:5173,http://localhost:8081,http://127.0.0.1:8081}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME:-}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY:-}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET:-}
      # Correo de bienvenida (Brevo). SMTP_FROM debe ser remitente verificado en Brevo.
      BREVO_API_KEY: ${BREVO_API_KEY:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      SMTP_FROM: ${SMTP_FROM:-metafit.sistema@gmail.com}
      URL_APK: ${URL_APK:-https://metafit-frontend-78x6.onrender.com/app/metafit.apk}
    ports:
      - "${PORT}:${PORT}"
    depends_on:
      db:
        condition: service_healthy # espera a que MySQL esté listo y responda ping
    networks:
      - metafit_net

  # ──────────────────────────────────────────────────────────
  # SERVICIO 3: Frontend React/Vite
  # ──────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend_web
      dockerfile: Dockerfile
    container_name: metafit_frontend
    restart: unless-stopped
    environment:
      - CHOKIDAR_USEPOLLING=true
      # URL pública del backend accesible desde el navegador del host
      - VITE_API_URL=http://localhost:${PORT}
    ports:
      - "5173:5173"
    volumes:
      - ./frontend_web:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - metafit_net

  # ──────────────────────────────────────────────────────────
  # SERVICIO 4: phpMyAdmin
  # Interfaz web para auditar visualmente la base de datos MySQL
  # Acceso: http://localhost:8080
  # Usuario: root  |  Contraseña: valor de DB_PASSWORD en .env
  # ──────────────────────────────────────────────────────────
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: metafit_phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db # <- nombre exacto del servicio MySQL en esta red
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    ports:
      - "8080:80" # phpMyAdmin escucha en el 80 interno; lo exponemos en el 8080 local
    depends_on:
      db:
        condition: service_healthy # arranca solo cuando MySQL ya responde el healthcheck
    networks:
      - metafit_net

  # ──────────────────────────────────────────────────────────
  # SERVICIO 5: n8n — Automatización y workflows
  # Acceso: http://localhost:5678
  # Usuario: admin  |  Contraseña: Admin123!
  # ──────────────────────────────────────────────────────────
  n8n:
    image: n8nio/n8n
    container_name: metafit_n8n
    restart: unless-stopped
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=Admin123!
      - GENERIC_TIMEZONE=America/Bogota
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - metafit_net

  # ──────────────────────────────────────────────────────────
  # SERVICIO 6 (SOLO DOCUMENTACIÓN): BD remota en Oracle VPS + Dokploy
  # Este servicio NO se levanta con `docker compose up`:
  # está en el profile "documentation-only" como referencia.
  # La BD REAL de producción ya vive en el VPS Oracle
  #   http://141.148.94.173:3000 (Dokploy) · MySQL 8 en 141.148.94.173:3306
  # Para ver la config equivalente en Dokploy revisá
  #   documentacion/infraestructura_vps.md
  # ──────────────────────────────────────────────────────────
  dokploy-db-remote:
    image: mysql:8.0
    container_name: metafit_db_remote_docs
    environment:
      MYSQL_ROOT_PASSWORD: Admin123!
      MYSQL_DATABASE: metafit
      MYSQL_USER: metafit
      MYSQL_PASSWORD: Admin123!
    # Este servicio NO se levanta, es solo documentación.
    # La BD real está en Oracle VPS: 141.148.94.173:3306
    profiles: ["documentation-only"]

# ──────────────────────────────────────────────────────────
# VOLUMES y NETWORKS
# ──────────────────────────────────────────────────────────
volumes:
  metafit_db_data:
    # Para borrar el volumen y re-inicializar la DB desde cero:
    # docker compose down -v && docker compose up --build
  n8n_data:

networks:
  metafit_net:
    driver: bridge

# Rutas
# Abre ahora tu documentación:
# http://localhost:3001/swagger
# http://localhost:3001/api-docs

# Frontend
# http://localhost:5173/

# phpMyAdmin (auditoría visual de la DB)
# http://localhost:8080
# Usuario: root  |  Contraseña: la definida en DB_PASSWORD del .env

# ============================================================
# RUTAS Y ENLACES DEL PROYECTO
# ============================================================
#
# ── DESARROLLO LOCAL ────────────────────────────────────────
# Backend (Swagger):     http://localhost:3001/api-docs
# Backend (Health):      http://localhost:3001/health
# Frontend web:          http://localhost:5173/
# phpMyAdmin local:      http://localhost:8080
# App móvil (Expo):      http://localhost:8081/
# n8n local:             http://localhost:5678
#
# ── PRODUCCIÓN ──────────────────────────────────────────────
# Backend:               https://metafit-backend-rr18.onrender.com
# Backend (Health):      https://metafit-backend-rr18.onrender.com/health
# Frontend web:          https://metafit-frontend-78x6.onrender.com
# APK descarga:          https://metafit-frontend-78x6.onrender.com/app/metafit.apk
# Adminer (ver BD prod): https://metafit-adminer.onrender.com
#
# ── VPS ORACLE CLOUD ────────────────────────────────────────
# Dokploy panel:         http://141.148.94.173:3000
# MySQL VPS:             141.148.94.173:3306
# SSH VPS:               ssh -i <llave.key> opc@141.148.94.173
# CloudBeaver (futuro):  http://141.148.94.173:8978
#
# ── CREDENCIALES DE ACCESO ──────────────────────────────────
# MySQL VPS:             user=metafit / pass=Admin123! / db=metafit
# Adminer:               mismo user/pass del MySQL VPS
# Dokploy panel:         email configurado por el usuario
#
# ── SERVICIOS EXTERNOS ──────────────────────────────────────
# Cloudinary cloud:      llaq9vyl
# Brevo (correos):       API + SMTP relay
# GitHub repo:           https://github.com/juanscarvajal04-droid/Equipo_Metafit
#
# ============================================================
